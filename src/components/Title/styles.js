import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 28,
    color: "#131313",
    fontWeight: "bold",
    textAlign: "center",
    margin: 10,
  },
});

export default styles;
